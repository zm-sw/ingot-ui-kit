# Automatický release vX.Y.Z po každém pushi do main — viz
# scripts/release.mjs (zdroj pravdy je registr doc stránek; epochu X
# zvedá jen commit `release!:`). Vlastní release commit se přeskakuje,
# jinak by workflow spouštěl sám sebe.
#
# Bump verze nejde do main přímo — main přímý push nepřijímá. Skript ho
# posílá pull requestem s auto-merge, a proto potřebuje víc než zápis do
# obsahu: založit PR a spustit ci.yml, aby na něm byly povinné checky.
name: release

on:
  push:
    branches: [main]

permissions:
  contents: write
  pull-requests: write
  actions: write

jobs:
  release:
    if: ${{ !startsWith(github.event.head_commit.message, 'chore(release):') }}
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@v4
        with:
          # Tagy a historie od posledního release — mělký checkout by
          # neviděl ani tag, ani commity k analýze.
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/release.mjs
